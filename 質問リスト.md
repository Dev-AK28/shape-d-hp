# 質問リスト

## 未回答

（なし）

## 解決済み

### config.godd の設定確認
プロジェクトスタックに合わせて `config.godd` を復元済み（`ja` / nextjs+react / vercel+github-actions）。

### 品質チェック指摘の Issue 化・実装
Issue #1〜#16 を実装し、PR #35 で main に統合済み。全 Issue クローズ済み。

### main ブランチ保護
GitHub API で有効化済み（`quality` チェック必須 + PR レビュー 1 名）。

### ブランチ保護 JSON
PR #44 で `.github/branch-protection.json` を main にマージ済み。

### CI/CD
GitHub Actions `quality` ジョブが main でグリーン。Actions を v5 に更新。

### Vercel デプロイ
- Production: https://www.shape-d.com
- 環境変数: `CONTACT_EMAIL`, `NEXT_PUBLIC_CONTACT_EMAIL`, `RESEND_FROM_NAME`（Production）
- Git 連携: `Dev-AK28/shape-d-hp` → `shape-d-hp`（production: `main`）

### Dependabot PR
- マージ済み: #39 framer-motion, #42 lucide-react, #43 dev-deps, #46 production-deps
- クローズ済み: #40 TypeScript 6（メジャー更新・別途検証）, #41 ESLint 10（CI 失敗）

### Resend / メール送信
- `RESEND_API_KEY` 設定済み（送信稼働中）
- 送信者表示 `shape-d-hp` を PR #48 で反映済み

### リポジトリ description / topics / Dependabot alerts
設定済み。
