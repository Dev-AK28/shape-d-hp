# 質問リスト

## 未回答

### config.godd の設定確認
以下の設定で問題ないか確認をお願いします（変更なければこのまま運用）。

- `display_language` / `main_language`: `ja`
- `frontend`: `[nextjs, react]`
- `infra`: `[vercel, github-actions]`

## 解決済み

### 品質チェック指摘の Issue 化・実装
Issue #1〜#16 を実装し、PR #35 で main に統合済み。全 Issue クローズ済み。

### main ブランチ保護
GitHub API で有効化済み（`quality` チェック必須 + PR レビュー 1 名）。

### ブランチ保護 JSON
PR #44 で `.github/branch-protection.json` を main にマージ済み。

### CI/CD
GitHub Actions `quality` ジョブが main でグリーン。Actions を v5 に更新（Node 20 非推奨警告の解消）。

### Vercel デプロイ
- Production デプロイ完了: https://www.shape-d.com
- 環境変数設定済み: `CONTACT_EMAIL`, `NEXT_PUBLIC_CONTACT_EMAIL`（Production / Preview / Development）
- Git 連携完了: `Dev-AK28/shape-d-hp` → Vercel プロジェクト `shape-d-hp`（production branch: `main`）

### Dependabot PR
古い main ベースの #33/#34 はクローズ。CI グリーンの PR は順次マージ中。

### リポジトリ description / topics / Dependabot alerts
設定済み。

### Resend / メール送信
- `RESEND_API_KEY` は設定済み（メール送信稼働中）
- 送信者表示名 `shape-d-hp` を From ヘッダー・件名・本文に反映（PR 予定）
