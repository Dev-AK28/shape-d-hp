/** Native dimensions of `public/shape-d-logo-transparent.png`. */
export const BRAND_LOGO_WIDTH = 1536;
export const BRAND_LOGO_HEIGHT = 1024;

/** Hero variant max width — keep in sync with `BrandLogo` and Hero logo stage. */
export const BRAND_LOGO_HERO_CLASS = 'w-[min(88vw,560px)]';

export const brandLogoHeroAspectRatio = `${BRAND_LOGO_WIDTH} / ${BRAND_LOGO_HEIGHT}`;
